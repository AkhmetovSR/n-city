import s from "./Category.module.css";
import { motion } from "framer-motion";

export default function Category() {
    return (
        <motion.div className={s.Category}>

        </motion.div>
    );
}
